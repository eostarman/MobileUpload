    import XCTest
    @testable import MobileUpload

    final class MobileUploadTests: XCTestCase {
        func testExample() {
            // This is an example of a functional test case.
            // Use XCTAssert and related functions to verify your tests produce the correct
            // results.
            XCTAssertEqual(MobileUpload().text, "Hello, World!")
        }
    }
